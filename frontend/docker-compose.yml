version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Production values (commented out for testing)
        NEXT_PUBLIC_APP_ROOT_API: https://api.flexora.com/api/v1
        NEXT_PUBLIC_BASE_API: https://api.flexora.com
        NEXT_PUBLIC_APP_URL: https://flexora.com
        NEXT_PUBLIC_BASE_URL: https://flexora.com
        NEXT_PUBLIC_FRONTEND_URL: https://flexora.com

        # Current testing values (using IP address)
        # NEXT_PUBLIC_APP_ROOT_API: http://109.199.119.157:5001/api/v1
        # NEXT_PUBLIC_BASE_API: http://109.199.119.157:5001
        # NEXT_PUBLIC_APP_URL: http://109.199.119.157:3001
        # NEXT_PUBLIC_BASE_URL: http://109.199.119.157:3001
        # NEXT_PUBLIC_FRONTEND_URL: http://109.199.119.157:3001
        NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY}
    container_name: flexora-frontend
    restart: unless-stopped
    ports:
      - '3001:3001'
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      - NODE_ENV=production
      - AUTH_SECRET=K8mN2pQ5rT9vW3xZ7bC1dF4gH6jL0nM8oP2qR5sT8uV1wX4yZ7aB0cD3eF6gH9
      - NEXTAUTH_URL=http://109.199.119.157:3001
    networks:
      - flexora-network

networks:
  flexora-network:
    name: flexora-network
    external: true
